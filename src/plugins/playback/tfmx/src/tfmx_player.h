/*
 *
 *
 */

#define NO_SCHED

#include "tfmx.h"


char player_TFMXVoices(TfmxState* state);  // Return the # of voices
void player_tfmxIrqIn(TfmxState* state);

//extern struct Pdblk pdb;
//extern int nSongs;

/* one Hdb per voice. 4 voices: 0,1,2,3; 7 voices: 4,5,6,7,0,1,2 */

//extern struct Hdb hdb[8];
//extern struct Cdb cdb[8];
//extern struct Mdb mdb;
//extern int jiffies;
